# 변경 이력

## 미배포 문서 수정 - 2026-08-24

- 공개 근거 없이 실제 MM/7240XM 동작을 운영자가 확인한 것으로 표시한 문구를
  제거했습니다. 자동 검증과 실제 장비·운영 환경의 별도 현장 증거 경계를 다시
  명확히 했으며 프로그램 코드와 장비 명령은 변경하지 않았습니다.

## 0.4.1 - 2026-08-24

### 안정성

- 성공, SSH timeout, 연결 끊김, 예기치 않은 Collector 예외, 지문 승인 거절·재시도를
  한 프로세스에서 반복하는 결정적 장애 주입 Soak를 추가했습니다. 기본 pytest는
  빠른 120회 반복을 포함하고 Windows CI·Release 빌드는 별도 1,000회 반복을 다시
  실행합니다.
- 연결 확인 중 여러 번 요청된 `지금 점검`을 연결 작업 종료 후 정확히 한 번 실행하도록
  수정했습니다. 이전에는 대기 플래그가 남아 다음 수동 점검 뒤 불필요한 추가 점검이
  실행될 수 있었습니다.
- Qt Worker pool이 작업 제출을 거부하는 예외 경로에서 `busy`, Worker 추적, 일회성
  연결 요청과 입력 자격 증명 override를 즉시 해제하고 사용자에게 안전한 실패를
  반환하도록 보강했습니다.
- 실제 fsync/원자 교체를 사용하는 설정 commit·rollback·중단 복구, 두 번째 자격
  증명 저장 실패 시 첫 번째 staged 값 정리, 여러 호스트 키의 전부 성공/전부 실패
  갱신, malformed 출력 Parser 경계를 반복 검증합니다.

### 검증 범위

- 실제 장비 명령·판정 로직은 변경하지 않았습니다. 공개 자동화는 비식별 실패 주입으로
  프로그램 상태 복구를 검증합니다. Aruba MM/7240XM 현장 동작은 공개 자동화로
  대체하지 않으며 별도 현장 증거가 필요한 상태로 관리합니다.

## 0.4.0 - 2026-08-23

### 변경

- 별도 MM·클러스터 연결 테스트를 없애고, SSH 연결 관련 설정을 저장할 때 전체
  장비의 공개 키를 자격 증명 조회 전에 먼저 스캔한 뒤 한 화면에서 일괄 승인하고
  로그인을 자동 확인하도록 통합했습니다. 연결과 무관한 운영·알림·화면 설정만
  바뀐 경우에는 네트워크 점검 없이 저장합니다.
- 최초 승인 키는 MM과 모든 응답 가능한 Controller를 하나의 원자적
  `known_hosts` 갱신으로 기록합니다. 이미 승인된 키와 다른 지문은 Primary나
  Fallback의 성공 여부와 관계없이 저장을 차단하며, 지문 스캔과 실제 인증 사이에
  키가 바뀌어도 strict SSH 검증으로 차단합니다.
- 저장 성공 조건을 MM 로그인 성공과 최소 1대의 Cluster Controller 로그인
  성공으로 명확히 했습니다. 나머지 Controller가 응답하지 않거나 로그인하지
  못해도 `Fallback 준비 미완료 N대` 경고와 장비별 결과를 표시하고 저장할 수
  있으며, `고급 진단 > 연결 다시 확인`에서 전체 상태를 다시 점검할 수 있습니다.
- 호스트 키 사전 스캔 단계의 안전한 알고리즘 협상 실패도
  `SSH_ALGORITHM_INCOMPATIBLE`로 분류하고 약한 legacy 알고리즘을 자동으로
  활성화하지 않습니다. 승인 대기·실패·취소 중에는 새 자격 증명을 저장하지 않고
  기존 설정과 자격 증명 참조를 유지합니다.

### 검증 경계

- 자동 검증은 비식별 키·IP, 가짜 SSH, 임시 `known_hosts`와 offscreen Qt를
  사용합니다. 실제 MM/7240XM의 지문, 계정 권한, Primary/Fallback 장애 조합,
  구형 ArubaOS 알고리즘 호환성과 Windows Credential Manager 정책은 승인된
  사내 환경에서 별도로 확인해야 합니다.

## 0.3.7 - 2026-08-13

### 변경

- 종료 요청이 새 점검을 영구 차단하고 재시도 대기를 깨우며, 진행 중인 IPv4
  TCP 연결과 활성 SSH 전송을 소켓 종료로 중단하도록 보강했습니다. 정리는 GUI
  스레드 밖에서 실행하며 worker thread를 강제로 종료하거나 프로세스를 강제
  종료하지 않습니다.
- Paramiko 5.0.0과 정확한 CPython 3.13.15 x64 표준 GIL runtime으로 Windows
  빌드·CI·패키징 계약을 전환했습니다. 구형 장비와 SSH 알고리즘 협상이 되지
  않으면 약한 알고리즘을 자동으로 다시 켜지 않고 비식별 오류 코드
  `SSH_ALGORITHM_INCOMPATIBLE`로 안내합니다.
- 최종 onedir verifier가 `_internal`의 Python DLL 집합과 `python313.dll`의
  AMD64·3.13.15 메타데이터를 독립 검사하도록 보강하고, 소스 빌드 백엔드는
  보안 수정 버전인 `setuptools 84.0.0`과 `wheel 0.46.2`로 고정했습니다.
- SQLite 시작 검증에 필수 기본 키, `NOT NULL`, `CHECK`, 인덱스 열·정렬 순서와
  partial index 조건을 추가했습니다. 저장 JSON은 크기·깊이·노드 수를 제한하고
  중복 키, 비유한 수, 잘못된 시간·자료형과 비밀 필드 이름을 실패 안전하게
  거부합니다.
- 설정·저장 payload의 비밀 필드 이름을 구분자와 camelCase 단위로 검사하고,
  로그의 전체 Authorization 값과 등록된 자격 증명을 마스킹합니다. 로그 파일
  기록 실패 시에도 마스킹 전 record가 표준 오류로 다시 출력되지 않습니다.
- 저장된 메인 창 위치와 설정·장비 상세·개발자 정보 창을 현재 사용 가능한
  화면 영역에 맞춰 배치하여 고배율과 다중 모니터 구성 변경 뒤 화면 밖에
  놓이지 않도록 보강했습니다.
- 비정상적으로 긴 숫자·표 행·터미널 제어 문자열과 장비의 명령 거부 응답을
  안전한 파싱 오류로 격리하고, 대용량 표 파싱 중에도 종료 요청을 주기적으로
  확인하도록 보강했습니다. 한 소스의 예상 밖 파서 오류는 다른 소스의 유효한
  점검 결과까지 폐기하지 않습니다.
- SSH 호스트 키 승인 대기 중인 일회성 연결 설정은 승인 거절·저장 실패·종료
  즉시 메모리에서 폐기합니다. SQLite 저장 장애가 장시간 이어져도 현재 사건과
  이미 저장된 사건의 최종 상태는 보존하면서 미저장 전환 이력은 최근 10,000건으로
  제한해 메모리 증가를 막습니다.
- 시작 시 기준값·탐지 상태·확인 대기 변화·활성 장애를 읽지 못하면 이를 빈
  최초 실행 상태로 오인하지 않습니다. 원본 SQLite는 변경 없이 닫고 자동 점검을
  중지한 격리 메모리 저장소로 전환해 복구 필요성을 안내합니다. 미등록 장비
  정리와 시작 inventory 검증은 한 transaction에서 수행해 실패 시 함께
  rollback하며, 활성 장애가 10,000건을 초과하면 조용히 일부만 복원하지 않고
  실패 안전하게 중지합니다. 이전 실행에서 남은 동일 논리 장애의 중복 활성
  행도 임의로 하나를 선택하지 않고 원본 DB를 보존한 채 격리합니다.

### 검증 경계

- 자동 검증은 비식별 fixture, 로컬 TCP/가짜 SSH, 임시 SQLite와 offscreen Qt를
  사용합니다. 실제 구형 Aruba의 SSH 알고리즘 호환성, 사내망 종료 지연,
  Python 미설치 클린 Windows 11, 물리 100%/125%/150% 배율·고대비·다중
  모니터와 24시간 이상 운전은 별도 현장 검수가 필요합니다.

## 0.3.6 - 2026-08-13

### 변경

- 모든 실행을 일반 사용자 모드로 시작하고, 애플리케이션이 수정 키 없는 직접
  `F12` 입력을 받았을 때만 개발자 UI 식별 모드를 켜거나 끄도록 추가했습니다.
  명령줄 인수, 환경 변수, 설정 파일, 일반 메뉴와 트레이 메뉴에는 활성화 경로가
  없으며 활성 상태를 저장하지 않습니다.
- 개발자 모드에서 고정된 한국어 이름, UI 식별자, 화면 위치, 저장소 상대 소스
  위치와 용도를 확인할 수 있습니다. `요소 선택` 중 클릭은 원래 버튼·표·탭·메뉴
  동작을 실행하지 않고 요소 정보만 열며, `Esc`는 선택만 취소하고 개발자 모드는
  유지합니다. 다시 `F12`를 누르면 선택 상태와 함께 개발자 모드가 꺼집니다.
- 작업 요청 복사는 정적 카탈로그의 버전·화면 위치·요소 이름·UI 식별자·
  저장소 상대 소스 위치·용도와 빈 작성란만 사용합니다. 실행 중인 IP,
  Hostname·장비 별칭과 행 값, 계정·자격
  증명, 설정값, 원본 명령 출력, 로그와 로컬 절대 경로는 읽거나 복사하지 않습니다.
  Windows 네이티브 트레이 메뉴 항목은 화면 오버레이 대신 `요소 목록`의 정적
  카탈로그에서 확인합니다.

### 검증 경계

- 자동 검증은 offscreen Qt 이벤트와 비식별 정적 메타데이터를 사용합니다. 실제
  Windows 11의 물리 F12/Fn 키 매핑, 창 포커스, 시스템 트레이, 클립보드 정책,
  고대비 테마, 100%/125%/150% 배율과 다중 모니터 동작은 패키지 현장 검수가
  필요합니다.

## 0.3.5 - 2026-08-13

### 변경

- 저사양 모드에서도 MM과 클러스터 수집을 최대 두 작업으로 동시에 실행하여,
  느린 MM 응답이 컨트롤러 상태와 Client 분배 상태 확인을 지연시키지 않도록
  개선했습니다. 세 조회 명령, timeout·재시도, 감지 규칙과 결과 정확성은
  일반 모드와 같습니다.
- 큰 원본 명령 출력은 일부 구간으로 압축 효과를 먼저 확인한 뒤 64Ki 문자 단위로
  처리합니다. 압축 효과가 낮은 출력은 원문을 그대로 보관하며, 손상된 압축
  항목은 다른 명령 출력과 분리해 안전한 오류 코드로 표시합니다.
- 저사양 모드의 전체 보기에 250대 단위 페이지를 적용했습니다. 전체 장비를
  먼저 정렬한 뒤 현재 페이지만 그리며, 작은 보기는 기존처럼 등록 컨트롤러
  4대를 한 화면에 표시합니다.
- 메인 화면에 저사양 모드와 실제 자동 점검 간격을 지속해서 표시하고, Demo에서
  바꾼 설정은 현재 Demo 실행에만 적용된다는 안내를 명확히 했습니다.

### 검증 경계

- 자동 검증은 비식별 fixture, 로컬 가짜 SSH, offscreen Qt와 Windows onedir
  패키지를 사용합니다. 실제 저사양 노트북·HDD, 느린 Aruba SSH와 24시간 이상
  운전은 별도 현장 검수가 필요합니다.

## 0.3.4 - 2026-08-12

### 변경

- 장비 점검 결과의 전체 보기와 작은 보기 표에서 선택한 행을 넓은 Windows
  파란색 채움 대신 시스템 팔레트 기반의 연한 중립 회색으로 표시하도록
  개선했습니다. 선택 중에도 장비 상태 아이콘과 정상·주의·장애 문구를 읽을 수
  있도록 상태별 표시를 유지합니다.
- 행 선택을 사용하는 알림 확인과 장비 상세 보기, 정렬 및 화면 모드 전환 뒤의
  선택 장비 복원 동작은 그대로 유지합니다.

### 검증 경계

- 자동화는 offscreen Qt와 비식별 fixture를 사용합니다. 실제 Windows 11 테마와
  고대비 모드, 100%/125%/150% 화면 배율에서의 선택 행 가독성은 패키지 현장
  검수가 필요합니다.

## 0.3.3 - 2026-08-12

### 변경

- 설정창과 장비 상세창의 모든 탭에서 넓은 Windows 파란색 선택 채움을 제거하고,
  시스템 팔레트 기반의 중립색 탭과 얇고 차분한 청회색 하단 표시선으로 현재 탭을
  구분하도록 개선했습니다.
- 마우스 선택뿐 아니라 Tab 및 좌우 방향키를 이용한 기존 키보드 탐색과 포커스
  표시는 그대로 유지합니다.

### 검증 경계

- 자동화는 offscreen Qt와 비식별 fixture를 사용합니다. 실제 Windows 11 테마,
  고대비 모드와 100%/125%/150% 화면 배율은 패키지 현장 검수가 필요합니다.

## 0.3.2 - 2026-08-12

### 변경

- GitHub가 새 Prerelease Draft를 REST 목록에서 일시적으로 누락하더라도 태그별
  GraphQL 조회로 numeric release ID를 확인하고, REST numeric-ID 조회로 동일
  Draft를 다시 검증한 뒤에만 자산 업로드와 게시를 진행하도록 릴리스 자동화를
  보강했습니다.
- 실패한 `v0.3.1` 게시 시도의 annotated tag는 이동하거나 삭제하지 않고 보존하며,
  수정된 자동화는 새 `v0.3.2` 태그와 Release에서만 사용합니다.

## 0.3.1 - 2026-08-12

### 추가

- 데이터 폴더별 단일 실행 보호를 추가해 두 프로그램이 같은 SQLite 상태와 장비를
  동시에 점검하거나 중복 알림을 보내지 않도록 했습니다.

### 변경

- 등록 장비, 현재 관측 장비, 활성 사건과 미확인 Connection-Type 변화는 보존하면서
  오래된 미등록 장비 상태를 180일·10,000개 한도로 원자적으로 정리합니다.
- 설정 파일과 JSON 구조의 읽기 한도를 적용하고, 현재 자격 증명과 일시 연결 테스트
  비밀값만 메모리에서 마스킹해 장시간 실행 시 누적을 제한합니다.
- 설정 JSON 저장과 런타임 반영 사이에 복구 마커를 두어 적용·롤백이 연속으로
  실패하거나 프로세스가 중단돼도 다음 시작에서 이전 설정을 복원합니다.
- 데이터 폴더 생성 실패와 미래 버전 SQLite를 작업 시작 전에 안전하게 안내하고,
  실패한 초기 연결을 닫아 원본 상태를 보존합니다.
- GitHub Actions를 공식 Node.js 24 기반 버전의 검증된 commit SHA로 갱신했습니다.
- 동결 UI smoke가 실제 대시보드와 fixture 워커를 한 회 실행하고 스레드 풀을
  정상 종료한 경우에만 lifecycle 검증 마커를 남기도록 강화했습니다.

### 검증 경계

- 자동화는 비식별 fixture, 로컬 가짜 SSH, offscreen Qt와 Windows onedir 패키지를
  사용합니다. 실제 Aruba 장비, 저사양 HDD 노트북과 장시간 현장 운용은 별도 검수가
  필요합니다.

## 0.3.0 - 2026-08-12

### 추가

- 노후 Windows 11 PC용 저사양 모드를 추가했습니다. 자동 점검은 최소 120초와
  MM/Cluster 순차 수집을 사용하며 수동 점검, 세 명령과 장애 판정은 유지합니다.
- 첫 실행 설정 안내, 간소화된 빠른 설정, 고급 설정 접기, 전체 설정 도움말과
  선택적 비식별 성능 로그를 추가했습니다.
- 성능 기준선과 현장 검증 경계를 기록한 보고서를 배포 패키지에 포함합니다.

### 변경

- SQLite 점검 상태를 일괄 저장하고 활성 사건만 메모리에 복원하며, 종료 이력은
  180일 및 10,000건 한도로 안전하게 정리합니다.
- 보이는 표만 갱신하고 아이콘·상세 창을 재사용하며 원본 탭과 설정 저장을
  지연해 반복 UI 객체와 HDD 쓰기를 줄였습니다.
- SSH 출력 누적 복사를 제거하고 작은 chunk, 출력 제한, pager, 취소와 재시도
  backoff 경로를 보강했습니다.
- Windows 패키지는 필요한 한국어 Qt 번역만 포함하도록 축소했습니다.
- SQLite 잠금 대기 시간을 UI에 적합한 짧은 범위로 제한하고, 앱 내부 저장 작업과
  겹친 설정 미러 쓰기는 즉시 보류해 장시간 화면 멈춤을 방지했습니다.
- 열린 상세창의 요약·파싱 결과·원본 출력이 점검 회차마다 함께 교체되도록 해
  장시간 실행 중 과거 원본 보관과 회차 혼합을 방지했습니다.
- 확인 완료 Connection-Type 변화 이력도 활성·확인 대기 상태를 보존하면서
  180일·10,000건 보관 정책에 포함했습니다.

## 0.2.0 - 2026-08-12

### 추가

- 기본 420x320 창에서는 등록된 컨트롤러 4대의 MM 보고 상태, Client 분배 상태와
  마지막 점검 시각만 보여 주는 간단 보기를 추가
- 창 최대화 또는 넓은 창에서 등록·미등록 장비의 전체 수집값을 보여 주는 전체
  보기와 1,000/900px 히스테리시스 기반 자동 전환 추가
- Controller/Client 분배 상태를 UI 문구와 분리한 구조화 상태 모델 추가
- 설정 수치와 감지 모드에서 직접 클릭한 뒤에만 마우스 휠 변경을 허용하는
  입력 안전 장치 추가

### 변경

- 장비·자격 증명에 등록된 Cluster 구성원 4개 IP만 장애 판단, 연속 감지,
  Connection-Type 사건과 알림의 감시 대상으로 사용
- MM에서 발견한 미등록 장비는 전체 보기의 정보 행으로 유지하되 감시 제외로
  표시하고 전체 심각도와 알림에는 반영하지 않음
- 표의 상세보기·알림 확인 연결을 행 인덱스가 아닌 장비 IP 기준으로 변경

### 검증 경계

- 자동화는 비식별 fixture, 가짜/로컬 SSH, offscreen Qt와 Windows onedir
  패키지를 사용합니다. 실제 Aruba 장비, Python 미설치 클린 Windows 11,
  실제 100%/125%/150% 화면 배율과 Windows 알림은 별도 현장 검수가 필요합니다.

## 0.1.1 - 2026-08-11

### 변경

- 런타임 Qt 의존성을 `PySide6-Essentials`로 축소하고 사용하지 않는 Qt
  Virtual Keyboard, PDF, QML/Quick 및 OpenGL 구성 요소를 배포 대상에서 제외
- Windows 실행 파일에 제품명, 파일 설명, 원본 파일명과 버전 리소스 추가
- 버전이 포함된 Windows x64 ZIP, SHA-256 파일 및 안전한 압축 해제 검증 경로 추가
- GitHub 수동 Windows Prerelease workflow와 일반 변경 검증 workflow 추가.
  불변 버전 태그, 테스트, 패키지 검증과 SHA-256 검증을 통과한 onedir ZIP만 게시
- 배포물의 제3자 고지와 런타임 라이선스 문서 구성 추가
- Aruba Mini Dashboard 자체 코드에 `Copyright (c) 2026 sebia1993` MIT License를
  적용하고, 배포물의 바이트 동일 `LICENSE.txt`를 필수 검증하도록 추가. 제3자
  구성요소의 저작권과 별도 라이선스는 그대로 보존
- 프로젝트 배포 조건이 LGPL 구성요소의 자체 사용 목적 수정 및 해당 수정
  디버깅을 위한 리버스 엔지니어링을 제한하지 않음을 문서화
- PySide6/shiboken6/Paramiko/scp를 PYZ 밖의 교체 가능한 원본 소스로 제공하고 exact source,
  라이선스 해시, Qt runtime inventory 및 교체·복구 안내를 검증하는 onedir 전용
  배포 경계 추가
- 설정·자격 증명·DB·로그·known_hosts·키·인증서·덤프 파일 및 안전하지 않은
  ZIP 경로를 거부하도록 패키지 verifier 강화
- 저장소 보안 제보 절차와 릴리스 운영 문서 추가

### 수정

- 최초 `v0.1.0` 배포물에 실제 UI에서 사용하지 않는 Qt Virtual Keyboard
  바이너리가 포함되던 문제 수정. 해당 GitHub Release는 Draft로 격리하고
  수정된 배포물은 새로운 `v0.1.1` 태그로만 생성
- Python/SSH/Credential 의존성, 동결 fixture와 Demo 종합 판단을 포함하는
  EXE smoke 검증을 ZIP 검증 단계에서도 수행

### 검증 경계

- 이 버전도 실제 Aruba 장비, Python 미설치 클린 Windows 11 일반 사용자
  PC/VM, 실제 100%/125%/150% 화면 배율과 조직 정책이 적용된 Windows 알림은
  별도 현장 검수가 필요하다. 이 검수가 끝나기 전에는 Prerelease로만 공개하며
  Stable Release로 표시하지 않는다.

## 0.1.0 - 2026-08-11

### 추가

- Aruba MM `show switches`와 Aruba 7240XM Cluster 두 명령의 분리형 수집기와
  견고한 표 파서
- IP별 `DeviceHealth`, 명시적 심각도 규칙, 주요/복수 문제 IP와 근거 표시
- Client 절대/상대 비교, 연속 활성·복구, 전체 저사용량·구성원 누락 처리
- 구성원 IP별 Connection-Type baseline, Primary/Fallback 전환을 넘는 비교,
  변경 사건, 확인과 재시작 유지
- 사건 활성·갱신·복구 journal과 동일 원인 알림 중복 방지
- Primary 실패 시 Fallback 순차 시도 및 실제 수집 Controller 기록
- Windows Credential Manager 영구 자격 증명과 세션 전용 자격 증명
- 사전 SSH SHA-256 지문 승인, 앱 전용 known_hosts와 변경 키 거부
- PySide6 한글 미니 대시보드, 설정·세부 정보, Worker Thread, 시스템 트레이
- 항상 위, 40~100% 투명도, 창 위치·크기와 알림 설정 저장
- 신규/반복/복구 Windows 트레이 알림, 선택적 알림음과 사용자 확인
- 운영 저장소와 분리된 8단계 fixture Demo 모드
- PyInstaller onedir 기본 빌드, 선택적 Console, 패키지 verifier와
  SHA-256 생성
- Windows 11 현장 검수 체크리스트와 보안·운영 문서

### 신뢰성 및 보안

- 허용된 세 show 명령 외 운영 명령 거부, 설정 모드 미사용
- 접속·명령·파싱 실패를 장비 Down으로 오판하지 않는 fail-closed 처리
- 설정 JSON 원자 저장, SQLite WAL·bounded lock 재시도와 손상 원본 보존
- baseline·변경·사건·journal의 단일 트랜잭션 저장 경로
- JSON·SQLite 비밀 필드 거부와 로그 중앙 마스킹
- 일반 로그의 명령 원문 제외, opt-in 파싱 오류 excerpt만 제한 기록
- PyInstaller 패키지에서 소스·DB·로그·자격 증명 파일과 필수 Qt 플러그인 검사

### 검증 경계

- 자동화는 비식별 fixture, 가짜/로컬 SSH, 임시 저장소와 offscreen UI를
  사용한다.
- 실제 Aruba 장비·ArubaOS 출력 호환성과 Python 미설치 클린 Windows 11
  PC/VM 검수는 아직 현장 증거가 필요하다.
